import 'package:flutter/material.dart';

class DividedNameScreen extends StatelessWidget {
  const DividedNameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}